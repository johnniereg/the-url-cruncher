<!DOCTYPE html>
<html lang="en">
<head>
  <title>The Link Cruncher</title>
</head>
<body>
  <% include ./partials/_header %>
  <h2>Crunch a link.</h2>
  <h3>Your crunched URLs</h3>
  <table style="width:75%">
  <% for (let url in urls) { %>
    <tr>
      <td><%= "Crunched link: " + url %></td>
      <td><a href="<%= urls[url] %>"> <%= urls[url] %></a></td>
      <td><form method="POST" action="/urls/<%= url %>/delete"><input type="submit" value="Delete"></form></td>
    </tr>
  <% } %>
  </table>
</body>
</html>