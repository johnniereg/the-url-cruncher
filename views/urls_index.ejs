<!DOCTYPE html>
<html lang="en">
<head>
  <title>The URL Cruncher</title>
</head>
<body style="font-family:sans-serif;padding:10px;background-color:darkgoldenrod">
  <% include ./partials/_header %>
  <h3>Your crunched URLs</h3>
  <table style="width:60%">
    <% for (let entry in urlCollection) { %>
      <tr>
        <td><b><%= entry %></b></td>
        <td><a href="<%= urlCollection[entry] %>"> <%= urlCollection[entry].longURL %></a></td>
        <td><form method="GET" action="/urls/<%= entry %>"><input type="submit" value="More"></form></td>
        <td><form method="POST" action="/urls/<%= entry %>?_method=DELETE"><input type="submit" value="Delete"></form></td>
      </tr>
    <% } %>
  </table>
  <br>
  <a href="urls/new"><button>Crunch a new URL</button></a>
</body>
</html>